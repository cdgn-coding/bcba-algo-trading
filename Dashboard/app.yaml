runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app --timeout 120
runtime_config:
  python_version: 3.6
manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 6
  disk_size_gb: 20
  volumes:
  - name: ramdisk1
    volume_type: tmpfs
    size_gb: 4
